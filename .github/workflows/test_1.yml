name: Ejemplo de Composite Action

on: [push]

jobs: 
  say-hello: 
    runs-on: ubuntu-latest

    env:
      MV_VAR: Mi variable de entorno
    
    steps:
      - name: Test
        env:
          STEP_MY_VAR: Nivel step
        run: echo "Valor.. $MV_VAR   Valor.. $STEP_MY_VAR"